// Phase 2 placeholder — Tauri commands wired in here
#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    tauri::Builder::default()
        .run(tauri::generate_context!())
        .expect("error while running Felix CAT");
}
